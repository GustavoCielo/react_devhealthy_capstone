import { motion } from "framer-motion";
import styled from "styled-components";

export const Loading = styled(motion.div)`
  width: 120px;
  height: 120px;
  margin: 40px auto;
  border-radius: 50%;
  width: 6em;
  height: 6em;
  border: 1.1em solid rgba(227, 101, 193, 0.2);
  border-left: 1.1em solid rgb(227, 101, 193);
`;
